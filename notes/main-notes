首先来看看标准的主函数写法是什么样的
在C99标准中规定的标准主函数写法：
                        int main(void) { /* ... */ }
或
                        int main(int argc, char *argv[]) { /* ... */ }
都是标准写法，其他的写法要么是过时的野路子，要么像void main()之类的邪恶写法会在编译器不报错的情况下让程序在你意想不到的地方崩溃。
其中
                        argc是命令行总的参数个数  
                        argv[]是argc个参数，其中第0个参数是程序的全名，余下的参数是命令行后面跟的用户输入的参数


请问这里为什么要设置这两个参数呢？不要不行吗？谢谢
有些时候程序运行时需要提供一些参数。比如copy命令，需要指明源文件和目标文件名，就得通过argc和argv来传递


因为main要返回给调用者一个值，所以void main是肯定不行的，即使编译通过了，编译器也会返回给系统一个值然后报一个warning。
至于int main()，虽然明确了返回值，但是参数列表却是空的，
在C语言里面跟C++有个区别就是对于参数列表为空的时候，C++表示这个函数没有参数，而C语言却表示这个函数可以传任意参数。
所以在C语言里面当一个函数不需要参数的时候都要明确加上void
